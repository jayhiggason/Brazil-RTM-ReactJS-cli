/*
 * RightDrawer Messages
 *
 * This contains all the text for the RightDrawer component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.RightDrawer";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RightDrawer component!"
  }
});
