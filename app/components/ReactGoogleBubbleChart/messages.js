/*
 * ReactGoogleBubbleChart Messages
 *
 * This contains all the text for the ReactGoogleBubbleChart component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ReactGoogleBubbleChart";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ReactGoogleBubbleChart component!"
  }
});
