/*
 * PerformanceLineApex Messages
 *
 * This contains all the text for the PerformanceLineApex component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.PerformanceLineApex";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PerformanceLineApex component!"
  },
  apex_x_axis_period_label: {
    id: `${scope}.apex_x_axis_period_label`,
    defaultMessage: "Period"
  },
});
