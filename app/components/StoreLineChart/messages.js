/*
 * StoreLineChart Messages
 *
 * This contains all the text for the StoreLineChart component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.StoreLineChart";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the StoreLineChart component!"
  }
});
