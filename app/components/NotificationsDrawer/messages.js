/*
 * NotificationsDrawer Messages
 *
 * This contains all the text for the NotificationsDrawer component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.NotificationsDrawer";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the NotificationsDrawer component!"
  }
});
