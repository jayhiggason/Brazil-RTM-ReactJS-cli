/*
 * CombinedMenu Messages
 *
 * This contains all the text for the CombinedMenu component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.CombinedMenu";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CombinedMenu component!"
  }
});
