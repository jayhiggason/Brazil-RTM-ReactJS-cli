/*
 * BrandTrendChart Messages
 *
 * This contains all the text for the BrandTrendChart component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.BrandTrendChart";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BrandTrendChart component!"
  }
});
