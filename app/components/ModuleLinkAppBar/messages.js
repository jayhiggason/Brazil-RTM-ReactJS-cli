/*
 * ModuleLinkAppBar Messages
 *
 * This contains all the text for the ModuleLinkAppBar component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ModuleLinkAppBar";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ModuleLinkAppBar component!"
  }
});
