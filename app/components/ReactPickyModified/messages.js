/*
 * ReactPickyModified Messages
 *
 * This contains all the text for the ReactPickyModified component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ReactPickyModified";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ReactPickyModified component!"
  }
});
