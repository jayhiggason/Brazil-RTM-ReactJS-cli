/*
 * PocTable Messages
 *
 * This contains all the text for the PocTable component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.PocTable";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PocTable component!"
  }
});
