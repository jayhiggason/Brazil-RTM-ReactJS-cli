/*
 * SideFilter Messages
 *
 * This contains all the text for the SideFilter component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.SideFilter";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the SideFilter component!"
  },
  search_store: {
    id: `${scope}.search_store`,
    defaultMessage: "Search Store"
  }
});
