/*
 * DistributorCard Messages
 *
 * This contains all the text for the DistributorCard component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.DistributorCard";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DistributorCard component!"
  }
});
