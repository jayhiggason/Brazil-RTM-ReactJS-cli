/*
 * ComingSoonPaper Messages
 *
 * This contains all the text for the ComingSoonPaper component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ComingSoonPaper";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ComingSoonPaper component!"
  }
});
