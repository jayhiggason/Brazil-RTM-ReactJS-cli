/*
 * HeaderRtm Messages
 *
 * This contains all the text for the HeaderRtm component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.HeaderRtm";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the HeaderRtm component!"
  }
});
