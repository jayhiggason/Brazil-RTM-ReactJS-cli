/*
 * ApexGroupedBarChart Messages
 *
 * This contains all the text for the ApexGroupedBarChart component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ApexGroupedBarChart";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ApexGroupedBarChart component!"
  }
});
