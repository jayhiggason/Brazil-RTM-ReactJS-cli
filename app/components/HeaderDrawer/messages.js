/*
 * HeaderDrawer Messages
 *
 * This contains all the text for the HeaderDrawer component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.HeaderDrawer";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the HeaderDrawer component!"
  }
});
