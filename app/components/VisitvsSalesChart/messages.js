/*
 * VisitvsSalesChart Messages
 *
 * This contains all the text for the VisitvsSalesChart component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.VisitvsSalesChart";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the VisitvsSalesChart component!"
  }
});
