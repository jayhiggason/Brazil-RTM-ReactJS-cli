/*
 * CollapsibleMenuList Messages
 *
 * This contains all the text for the CollapsibleMenuList component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.CollapsibleMenuList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CollapsibleMenuList component!"
  }
});
