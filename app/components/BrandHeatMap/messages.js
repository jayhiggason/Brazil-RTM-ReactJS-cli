/*
 * BrandHeatMap Messages
 *
 * This contains all the text for the BrandHeatMap component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.BrandHeatMap";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BrandHeatMap component!"
  }
});
