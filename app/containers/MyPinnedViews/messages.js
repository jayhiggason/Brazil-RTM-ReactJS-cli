/*
 * MyPinnedViews Messages
 *
 * This contains all the text for the MyPinnedViews container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.MyPinnedViews";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the MyPinnedViews container!"
  }
});
