/*
 * PocView Messages
 *
 * This contains all the text for the PocView container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PocView";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PocView container!"
  }
});
