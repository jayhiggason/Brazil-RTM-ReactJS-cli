// import { selectUserPreferencesDomain } from '../selectors';

describe("selectUserPreferencesDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(true);
  });
});
