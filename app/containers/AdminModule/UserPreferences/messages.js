/*
 * UserPreferences Messages
 *
 * This contains all the text for the UserPreferences container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.UserPreferences";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the UserPreferences container!"
  }
});
