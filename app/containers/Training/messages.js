/*
 * Training Messages
 *
 * This contains all the text for the Training container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Training";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Training container!"
  }
});
